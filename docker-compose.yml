version: '3.8'
services:
  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: FitnessApp_DB
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=Haslomaslo123
    ports:
      - "1435:1433"
    volumes:
      - sql_data:/var/opt/mssql

volumes:
  sql_data: